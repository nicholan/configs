{
  "name": "configs",
  "version": "1.0.0",
  "private": true,
  "description": "Configuration files",
  "keywords": [],
  "scripts": {
    "build:eslint": "cd ./eslint-config-custom && npm run prepack",
    "build:prettier": "cd ./prettier-config-custom && npm run prepack",
    "build:all": "npm run build:eslint && npm run build:prettier"
  },
  "author": "Nicholas Anttila",
  "license": "ISC",
  "dependencies": {
    "@nicholan/prettier-config-custom": "latest",
    "prettier": "latest"
  },
  "prettier": "@nicholan/prettier-config-custom",
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
